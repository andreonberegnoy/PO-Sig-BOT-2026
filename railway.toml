# Railway deploy config for the direct PO-API bot (no Chrome).

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python3 main.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
