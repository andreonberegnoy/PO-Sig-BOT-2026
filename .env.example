# Railway / Docker env vars (copy to .env for local testing)
# NEVER commit real values — this file is just a template.

MODE=paper                 # paper | real
CDP_URL=http://localhost:9222

# po-signals credentials (required on Railway; optional locally if already logged in)
PO_EMAIL=
PO_PASSWORD=

# Telegram
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
